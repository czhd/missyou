spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: none

missyou:
  api-package: pro.kelu.missyou.api
  jackson:
    property-naming-strategy: SNAKE_CASE
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: true
